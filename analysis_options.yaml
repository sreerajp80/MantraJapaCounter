# Static analysis configuration.
#
# Baseline: package:flutter_lints, plus the stricter rule set required by
# docs/guidelines/flutter_project_engineering_standard.md section 16.1.
#
# `flutter analyze` must report 0 issues before any commit.

include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Generated code — regenerate instead of editing, so do not lint it.
    - lib/l10n/app_localizations*.dart
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    # Correctness and safety
    avoid_print: true
    cancel_subscriptions: true
    close_sinks: true
    no_duplicate_case_values: true
    avoid_empty_else: true
    use_key_in_widget_constructors: true

    # Style and consistency
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_is_empty: true
    unnecessary_brace_in_string_interps: true
    unnecessary_this: true
    avoid_redundant_argument_values: true
    use_full_hex_values_for_flutter_colors: true
    use_enums: true
    noop_primitive_operations: true
    avoid_bool_literals_in_conditional_expressions: true

    # Widget structure
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    sort_child_properties_last: true
    use_decorated_box: true

    # Imports — package: imports only, never relative
    always_use_package_imports: true
