# Upgrade SDK constraints and documentation for Flutter 3.44.8 / Dart 3.12.2

**Status:** completed

## The issue

The Flutter SDK environment on the system has been updated to **Flutter 3.44.8** (channel stable, revision `058e0af2c2`) which includes **Dart 3.12.2** and **DevTools 2.57.0**.
The repository configurations (`pubspec.yaml`) and project documentation (`AGENTS.md`, `CLAUDE.md`, `docs/features.md`, `docs/implementation_progress.md`) currently reference the previous Flutter 3.41 / Dart 3.11.5 baseline (`sdk: ^3.11.5`).

## The fix

1. Update `pubspec.yaml` Dart SDK constraint to `^3.12.2`.
2. Update Flutter / Dart SDK references across project identity and documentation files (`AGENTS.md`, `CLAUDE.md`, `docs/features.md`, `docs/implementation_progress.md`).
3. Run `flutter pub get` to refresh pubspec dependencies lock file.
4. Run `flutter analyze` and `flutter test` to ensure 0 static analysis issues and all unit/widget tests pass on Flutter 3.44.8.

## Files to change

1. `pubspec.yaml` — update SDK constraint to `^3.12.2`.
2. `AGENTS.md` — update Flutter SDK to 3.44.8 and Dart SDK to `^3.12.2`.
3. `CLAUDE.md` — update Flutter SDK to 3.44.8 and Dart SDK to `^3.12.2`.
4. `docs/features.md` — update SDK reference to `sdk ^3.12.2`.
5. `docs/implementation_progress.md` — update Flutter/Dart SDK line to `3.44.8+ / Dart 3.12.2+`.

## Verification

- `flutter pub get`
- `flutter analyze` (must be 0 warnings / 0 errors)
- `flutter test` (all tests passing)
