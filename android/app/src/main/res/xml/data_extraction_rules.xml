<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup is disabled for this app (android:allowBackup="false").
  These rules make that explicit for Android 12+ (API 31) cloud backup and
  device-to-device transfer, so no counter names, session counts, or notes
  ever leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
